 :::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::
:GetDate yy mm dd
::
:: 作者：Ritchie Lawrence, 2002-06-15. 版本 1.0
::
:: 功能：把本地系统日期赋值给参数 1 到 3。
::       适用于 NT4/2000/XP/2003.
:: 
:: 参数：%1 该变量用于接收年，四位（引用调用）
::       %2 该变量用于接收月，两位，01 到 12（引用调用）
::       %3 该变量用于接收日，两位，01 到 31（引用调用）
:::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::
setlocal&set date=
for /f "tokens=1-4 delims=0123456789" %%a in ("%date%")do set delims=%%a%%b%%c%%d
for /f "tokens=1-3 delims=%delims: =% " %%a in ("%date%")do (
endlocal&set %1=%%a&set %2=%%b&set %3=%%c&goto :EOF)
:::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::  